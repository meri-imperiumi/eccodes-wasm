name: Build WASM

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  build:
    name: Build (${{ matrix.feature }}${{ matrix.release && ' (release)' || '' }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        feature: [basic, jpg]
        release: [false, true]
        exclude:
          - feature: basic
            release: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install Emscripten
        uses: emscripten-core/setup-emsdk@v15
        with:
          version: '6.0.8'

      - name: Install CMake
        uses: jwlawson/actions-setup-cmake@v2
        with:
          cmake-version: '3.27.x'

      - name: Setup ecCodes
        run: |
          ECCODES_VERSION=$(cat ECCODES_VERSION)
          echo "Building ecCodes $ECCODES_VERSION"
          if [ ! -d eccodes/CMakeLists.txt ]; then
            git clone --depth 1 --branch "$ECCODES_VERSION" https://github.com/ecmwf/eccodes.git eccodes
          fi

      - name: Build
        run: |
          if [ "${{ matrix.feature }}" = "jpg" ]; then
            if [ "${{ matrix.release }}" = "true" ]; then
              make release
            else
              make build-jpg
            fi
          else
            make build
          fi

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: eccodes-wasm-${{ matrix.feature }}${{ matrix.release && '-release' || '' }}
          path: build/eccodes/
          retention-days: 7

  test:
    name: Test (${{ matrix.os }} - ${{ matrix.feature }})
    needs: build
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        feature: [basic, jpg]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Setup ecCodes
        shell: bash
        run: |
          ECCODES_VERSION=$(cat ECCODES_VERSION)
          if [ ! -d eccodes/tests/grib_to_gridspec ]; then
            git clone --depth 1 --branch "$ECCODES_VERSION" https://github.com/ecmwf/eccodes.git eccodes
          fi

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: eccodes-wasm-${{ matrix.feature }}
          path: build/eccodes

      - name: Get WASM size
        shell: bash
        run: |
          echo "Build configuration: ${{ matrix.feature }}"
          ls -lh build/eccodes/eccodes.wasm 2>/dev/null || echo "WASM file not found"
          wc -c < build/eccodes/eccodes.wasm 2>/dev/null || echo "0"

      - name: Test
        run: node --test test/basic.mjs test/e2e.mjs

      - name: Check Build
        run: node scripts/prepublish-check.js

  size-report:
    name: Size Report
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Generate Report
        run: |
          echo "# WASM Build Sizes" > size-report.md
          echo "" >> size-report.md
          echo "| Configuration | Size (MB) |" >> size-report.md
          echo "|--------------|----------|" >> size-report.md
          for dir in artifacts/*/; do
            name=$(basename "$dir")
            size=$(du -m "$dir/eccodes.wasm" | cut -f1)
            echo "| $name | $size MB |" >> size-report.md
          done
          cat size-report.md

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('size-report.md', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: report
            })
